import React from "react";
import loadable from "@loadable/component";

const Banner = () => {
  return (
    <div>
      <span>Banner Female Fertility</span>
    </div>
  );
};

export default Banner;
